query v2JbdaoRedemptions {
  projects(where: { id: "2-1" }) {
    redeemEvents(orderBy: timestamp, orderDirection: desc) {
      timestamp
      returnAmount
      amount
      caller
      beneficiary
      txHash
    }
  }
}
